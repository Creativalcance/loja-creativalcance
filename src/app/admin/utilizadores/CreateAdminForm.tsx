"use client";
import { useActionState } from "react";
import { createAdminAction } from "./actions";
export default function CreateAdminForm(){const[state,action,pending]=useActionState(createAdminAction,{success:false,message:""});return <form action={action} className="grid gap-4 rounded-3xl border border-white/10 bg-white/[.03] p-6 md:grid-cols-3"><input name="full_name" required placeholder="Nome completo" className="rounded-xl bg-white px-4 py-3 text-neutral-950"/><input name="email" type="email" required placeholder="E-mail" className="rounded-xl bg-white px-4 py-3 text-neutral-950"/><input name="password" type="password" minLength={8} required placeholder="Palavra-passe inicial" className="rounded-xl bg-white px-4 py-3 text-neutral-950"/>{state.message?<p className={`text-sm md:col-span-2 ${state.success?"text-emerald-300":"text-red-300"}`}>{state.message}</p>:<span/>}<button disabled={pending} className="rounded-xl bg-white px-5 py-3 font-semibold text-neutral-950 disabled:opacity-60">{pending?"A criar...":"Criar Admin"}</button></form>}
