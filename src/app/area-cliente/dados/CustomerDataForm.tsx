"use client";
import { useActionState } from "react";
import { updateCustomerDataAction } from "./actions";
export default function CustomerDataForm({fullName, email, phone}:{fullName:string;email:string;phone:string}){const [state,action,pending]=useActionState(updateCustomerDataAction,{success:false,message:""});return <form action={action} className="mt-8 space-y-5"><div><label className="text-sm font-medium">Nome completo</label><input name="full_name" defaultValue={fullName} required className="mt-2 w-full rounded-2xl border border-neutral-300 px-4 py-3"/></div><div><label className="text-sm font-medium">E-mail</label><input value={email} disabled className="mt-2 w-full rounded-2xl border border-neutral-200 bg-neutral-100 px-4 py-3"/></div><div><label className="text-sm font-medium">Telefone</label><input name="phone" defaultValue={phone} className="mt-2 w-full rounded-2xl border border-neutral-300 px-4 py-3"/></div>{state.message?<div className={`rounded-2xl px-4 py-3 text-sm ${state.success?"bg-emerald-50 text-emerald-700":"bg-red-50 text-red-700"}`}>{state.message}</div>:null}<button disabled={pending} className="rounded-2xl bg-neutral-950 px-5 py-3 text-sm font-semibold text-white">{pending?"A guardar...":"Guardar dados"}</button></form>}
